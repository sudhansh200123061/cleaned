question extern join command  set unix box ide scsi drive wonder use m join command like way set thing sinc stick c e crap enforc like would mimic mount point unix filesystem heard problem use least older version plea tell   steeven vawter telephon 